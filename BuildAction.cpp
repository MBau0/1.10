#include "BuildAction.h"

BuildAction::BuildAction(Entity* entity, int id, std::string name, GLuint image) :
	Action		( entity, id, name, image )
{}

void BuildAction::execute() {
	// something something
	// vector of actions 
}